# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Async database
sqlalchemy[asyncio]>=2.0.25
aiosqlite>=0.19.0
asyncpg>=0.29.0          # PostgreSQL async driver (optional)

# LLM integration
openai>=1.12.0           # OpenAI-compatible API client

# SQL parsing for validator
sqlparse>=0.4.4

# Visualization (used by standalone visualizer.py)
matplotlib>=3.8.0

# Data validation (bundled with FastAPI, pinned for safety)
pydantic>=2.6.0
